
@"
{
  "version": 2,
  "buildCommand": "cd my-app && npm run build",
  "outputDirectory": "my-app/.next",
  "installCommand": "cd my-app && npm install",
  "framework": "nextjs"
}
"@ | Out-File -FilePath "vercel.json" -Encoding UTF8

# Add and commit vercel.json
git add vercel.json
git commit -m "Add vercel.json for proper deployment configuration"
git push origin main